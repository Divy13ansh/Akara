[project]
name = "akara-db"
version = "0.1.0"
description = "Akara shared DB models, engine and enums (SQLAlchemy 2.0 async)"
requires-python = ">=3.11"
dependencies = [
  "sqlalchemy>=2.0.30",
  "asyncpg>=0.29",
]

