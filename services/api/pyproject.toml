[project]
name = "akara-api"
version = "0.1.0"
description = "Akara API (tokens, library, progress, webhooks)"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
  "python-dotenv",
  "livekit-api",
  "httpx",
]

[tool.ruff]
line-length = 100
