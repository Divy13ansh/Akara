[project]
name = "akara"
version = "0.1.0"
description = "Akara monorepo (voice + video + api)"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = [
  "services/*",
  "packages/*",
]

[dependency-groups]
dev = ["pytest", "ruff", "httpx", "fastapi", "python-dotenv", "openai", "requests"]

[tool.ruff]
line-length = 100

